# Security

Do not open public issues containing API keys, credentials, cookies, private exports, or customer data.

Use environment variables for secrets:

```bash
export PARSESHELF_API_KEY="..."
```

If you need to report a sensitive issue, contact:

```text
info@parseshelf.com
```
